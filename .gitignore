# Compiled binaries and build artifacts
build/
install/
log/
CMakeFiles/
CMakeCache.txt

# Model files — trained .pth weights are large and not tracked; the .pte/.onnx
# artifacts are produced by the export scripts (see models/README.md).
models/*.pth
models/*.pte
models/*.onnx

# Collected datasets (produced by data_collection)
/dataset/

.export_venv/

# Python
__pycache__/
*.pyc
*.pyo

.venv/

.cache/
